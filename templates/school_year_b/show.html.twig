{% extends 'base.html.twig' %}

{% block title %}SchoolYearB{% endblock %}

{% block body %}
    <h1>SchoolYearB</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school_year_b.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school_year_b.name }}</td>
            </tr>
            <tr>
                <th>DateStart</th>
                <td>{{ school_year_b.dateStart ? school_year_b.dateStart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ school_year_b.dateEnd ? school_year_b.dateEnd|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('school_year_b_index') }}">back to list</a>

    <a href="{{ path('school_year_b_edit', {'id': school_year_b.id}) }}">edit</a>

    {{ include('school_year_b/_delete_form.html.twig') }}
{% endblock %}
